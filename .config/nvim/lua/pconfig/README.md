# Package configuration

This directory is only for Neovim plugin specifications and their configuration.
Application logic, commands, and general-purpose modules belong directly under
`lua/` (or in a more specific non-`pconfig` subdirectory).
